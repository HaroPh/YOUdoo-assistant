# B4 — PDF bóc bảng Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sửa lỗi 4 của spec gốc ("PDF gắn số sang hàng bên cạnh") bằng cách trích bảng qua
`pdfplumber` thay vì đọc dòng thô, và đảm bảo mỗi hàng bảng thành một chunk tự đủ nghĩa không bao
giờ bị gộp lẫn hàng khác.

**Architecture:** Một module lá mới `pdf_table.py` (hàm thuần, nhận `list[list]`, không biết
`pdfplumber`) làm mọi việc "thuật toán bảng": gộp hai chế độ trích xuất, tách khối header, đặt tên
cột, dựng text tự đủ nghĩa, kiểm checksum. `parse_pdf` gọi module này CHỈ trên trang có bảng
(`page.find_tables()` rỗng thì đi nguyên đường `pypdf` như hôm nay — bất biến byte-identical).
`chunking.py` học thêm khái niệm block "nguyên tử" (không gộp, không cắt câu). `ingest.py` nối cảnh
báo checksum vào cơ chế `Warning` đã có từ Kế hoạch 2.

**Tech Stack:** Python 3.11, `pdfplumber` (mới khai, đã cài `.venv` sẵn — ghim `0.11.10`), tái dùng
`is_column_index_row` từ `xlsx_header.py`, `pytest` (+ `@pytest.mark.live` cho corpus thật).

**Spec:** `docs/superpowers/specs/2026-09-04-b4-pdf-bang-design.md` (spec rộng hơn: mục 5.4 của
`docs/superpowers/specs/2026-08-29-tang-nap-tai-lieu.md`)

## Global Constraints

- KHÔNG đụng `heading_level()`, `_HEADING_RE`, `detect_page_furniture()` logic bên trong — chỉ đổi
  ĐẦU VÀO của chúng cho trang có bảng (spec §3.2, §5).
- Trang KHÔNG có bảng: output `parse_pdf` phải BYTE-IDENTICAL với trước B4 — phép thử phá bắt buộc
  cho gate này (spec §3.1, §6).
- `.docx`/`.pptx`/`.xlsx` không đổi hành vi: khoá `atomic` trong block dict mặc định `False`/vắng
  mặt, không caller nào khác đặt nó (spec §3.5, §5).
- Không viết lại predicate "hàng đánh số cột" — tái dùng `is_column_index_row(row: list) -> bool`
  từ `backend/src/rag/xlsx_header.py` nguyên trạng (spec §3.4).
- Báo cáo checksum đứt đoạn đi qua `Warning(path, where, reason)` đã có ở
  `backend/src/rag/ingest_report.py` — không phát minh cơ chế mới (spec §3.6).
- Chạy test: `pytest -m "not integration and not live"` cho vòng nhanh; `@pytest.mark.live` cho
  test cần thư mục corpus thật `d:/Youdoo/tmp-docs` (bỏ qua nếu không có, không fail cứng).
- Baseline hiện tại (đo 2026-09-04, đầu B4): `pytest --collect-only -q -m "not integration and not
  live"` → **2305/2381 tests collected**.

---

## Task 1: `pdf_table.py` — module lá, thuật toán bảng thuần

**Files:**
- Create: `backend/src/rag/pdf_table.py`
- Test: `backend/tests/rag/test_pdf_table.py`

**Interfaces:**
- Consumes: `is_column_index_row(row: list) -> bool` từ `backend/src/rag/xlsx_header.py` (đã có,
  không sửa).
- Produces (dùng ở Task 2):
  - `merge_table_rows(default_rows: list[list], text_rows: list[list]) -> list[tuple[list, bool]]`
    — trả list `(row, tu_fallback)`, `tu_fallback=True` nếu hàng chỉ chế độ `text` thấy được.
  - `split_header_body(rows: list[list]) -> tuple[list[list], list[list], bool]` — trả
    `(header_rows, body_rows, dung_mac_dinh)`.
  - `column_names(header_rows: list[list]) -> list[str]` — tên cột, 1-indexed khi không đọc được
    (`"Cột 3"`).
  - `row_to_text(row: list, columns: list[str]) -> str` — `"Cột1: giá_trị1 | Cột2: giá_trị2 | ..."`.
  - `checksum_gap(body_rows: list[list]) -> str | None` — `None` nếu không có cột đếm liên tục
    HOẶC không đứt đoạn; chuỗi mô tả chỗ đứt nếu có.

- [ ] **Step 1: Viết test cho `merge_table_rows` — cả hai chế độ khớp hoàn toàn**

```python
# backend/tests/rag/test_pdf_table.py
from src.rag.pdf_table import merge_table_rows


def test_merge_ca_hai_che_do_khop_hoan_toan():
    default_rows = [["1", "Mã A", "10%"], ["2", "Mã B", "5%"]]
    text_rows = [["1", "Mã A", "10%"], ["2", "Mã B", "5%"]]
    merged = merge_table_rows(default_rows, text_rows)
    assert merged == [(["1", "Mã A", "10%"], False), (["2", "Mã B", "5%"], False)]
```

- [ ] **Step 2: Chạy test, xác nhận FAIL vì chưa có hàm**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: FAIL với `ModuleNotFoundError: No module named 'src.rag.pdf_table'`

- [ ] **Step 3: Viết `merge_table_rows` — thuật toán 2 con trỏ, neo bằng ô đầu không rỗng**

```python
# backend/src/rag/pdf_table.py
"""Thuật toán bảng PDF — module LÁ, chỉ nhận lưới `list[list]`, không biết
`pdfplumber`. Tách khỏi `parse.py` vì `parse.py` phải giữ nguyên logic
`heading_level()`/furniture cho trang không bảng — trộn hai việc vào một
hàm sẽ khó chứng minh "trang không bảng không đổi gì" (spec §3.1)."""
import re

from .xlsx_header import is_column_index_row


def _to_text(v) -> str:
    return str(v).strip() if v is not None else ""


def _anchor(row: list) -> str:
    """Nội dung ô KHÔNG RỖNG đầu tiên của hàng — dùng làm điểm neo đối chiếu
    hai chế độ trích xuất. Không dùng chỉ số vị trí thuần: chế độ mặc định
    có thể thiếu hàng ở GIỮA danh sách, làm lệch chỉ số của mọi hàng sau đó
    (spec §3.3)."""
    for c in row:
        t = _to_text(c)
        if t:
            return t
    return ""


def merge_table_rows(default_rows: list[list], text_rows: list[list]
                     ) -> list[tuple[list, bool]]:
    """Gộp hai chế độ trích xuất `pdfplumber` của MỘT bảng.

    Neo bằng `_anchor()`. Nhìn trước 1 bước để phân biệt "text có hàng thừa"
    (mặc định làm mất hàng vắt trang — spec đo 194/210) với ca đối xứng hiếm
    "mặc định có hàng thừa". Khi neo trống ở CẢ HAI (ô đầu rỗng cả hai chế
    độ) coi là khớp theo vị trí — chấp nhận rủi ro lệch hiếm, ghi nhận là
    giới hạn đã biết."""
    i, j = 0, 0
    out: list[tuple[list, bool]] = []
    while i < len(default_rows) or j < len(text_rows):
        if i >= len(default_rows):
            out.append((text_rows[j], True))
            j += 1
            continue
        if j >= len(text_rows):
            out.append((default_rows[i], False))
            i += 1
            continue
        if _anchor(default_rows[i]) == _anchor(text_rows[j]):
            out.append((default_rows[i], False))
            i += 1
            j += 1
            continue
        # Nhìn trước: text có hàng thừa ở j (mặc định làm mất) hay mặc định
        # có hàng thừa ở i (đối xứng, hiếm)?
        if (j + 1 < len(text_rows)
                and _anchor(default_rows[i]) == _anchor(text_rows[j + 1])):
            out.append((text_rows[j], True))
            j += 1
            continue
        if (i + 1 < len(default_rows)
                and _anchor(default_rows[i + 1]) == _anchor(text_rows[j])):
            out.append((default_rows[i], False))
            i += 1
            continue
        # Không tìm được điểm neo trước — không đoán thêm, ưu tiên chế độ
        # mặc định (ô đầy đủ hơn) và tiến cả hai con trỏ.
        out.append((default_rows[i], False))
        i += 1
        j += 1
    return out
```

- [ ] **Step 4: Chạy lại test, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS

- [ ] **Step 5: Thêm test cho hàng vắt trang (text có hàng thừa) và ca đối xứng**

```python
def test_merge_text_co_hang_thua_vi_mac_dinh_mat_hang_vat_trang():
    default_rows = [["1", "Mã A", "10%"], ["3", "Mã C", "7%"]]
    text_rows = [["1", "Mã A", "10%"], ["2", "Mã B", ""], ["3", "Mã C", "7%"]]
    merged = merge_table_rows(default_rows, text_rows)
    assert merged == [
        (["1", "Mã A", "10%"], False),
        (["2", "Mã B", ""], True),
        (["3", "Mã C", "7%"], False),
    ]


def test_merge_mac_dinh_co_hang_thua_doi_xung():
    default_rows = [["1", "Mã A", "10%"], ["2", "Mã B", "5%"], ["3", "Mã C", "7%"]]
    text_rows = [["1", "Mã A", "10%"], ["3", "Mã C", "7%"]]
    merged = merge_table_rows(default_rows, text_rows)
    assert merged == [
        (["1", "Mã A", "10%"], False),
        (["2", "Mã B", "5%"], False),
        (["3", "Mã C", "7%"], False),
    ]
```

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS (cả hai)

- [ ] **Step 6: Viết test cho `split_header_body` — 5 nhánh của quy tắc header (spec §3.4)**

```python
from src.rag.pdf_table import split_header_body

# Bảng thật bieumau_bctc_hopnhat.pdf trang 1, rút gọn còn 3 cột để dễ đọc.
_BANG_BCTC = [
    ["", "Thuyết", "Số cuối"],
    ["TÀI SẢN", None, None],
    [None, "minh", "năm (3)"],
    ["", None, None],
    ["1", "2", "3"],                          # hàng đánh số cột
    ["I. Tiền", "100", "1.234.567"],          # hàng dữ liệu đầu tiên
    ["1. Tiền mặt", "111", "234.567"],
]


def test_split_header_body_nhieu_dong_va_hang_danh_so_cot():
    header, body, dung_mac_dinh = split_header_body(_BANG_BCTC)
    assert header == _BANG_BCTC[:5]
    assert body == _BANG_BCTC[5:]
    assert dung_mac_dinh is False


def test_split_header_body_hang_danh_so_cot_khong_bi_hieu_nham_du_lieu():
    # Tự nó: chỉ đưa đúng 1 hàng đánh số cột — không có hàng dữ liệu thật nào
    # theo sau, nên chốt an toàn phải kích hoạt (không được coi hàng đánh số
    # cột LÀ dữ liệu).
    rows = [["1", "2", "3"]]
    header, body, dung_mac_dinh = split_header_body(rows)
    assert dung_mac_dinh is True
    assert body == rows          # chốt an toàn: hàng DUY NHẤT thành thân


def test_split_header_body_bang_toan_chu_khong_tin_hieu_so_kich_chot_an_toan():
    rows = [["TT", "Biểu mẫu", "Tên báo cáo"], ["Ghi chú", "chung", "cho form"]]
    header, body, dung_mac_dinh = split_header_body(rows)
    assert dung_mac_dinh is True
    assert header == rows[:1]
    assert body == rows[1:]


def test_split_header_body_mot_hang_duy_nhat_khong_tin_hieu_so_van_thanh_than():
    # Bảng vỡ trang: pdfplumber trả 1 hàng duy nhất, không có gì trước nó để
    # làm header. Header rỗng còn hơn nuốt mất hàng đó vào "header" và không
    # bao giờ phát ra như dữ liệu (spec §3.4 chốt an toàn).
    rows = [["Cộng", "...", "..."]]
    header, body, dung_mac_dinh = split_header_body(rows)
    assert header == []
    assert body == rows
    assert dung_mac_dinh is True
```

- [ ] **Step 7: Chạy test, xác nhận FAIL**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: FAIL với `ImportError: cannot import name 'split_header_body'`

- [ ] **Step 8: Viết `split_header_body`**

```python
# Thêm vào backend/src/rag/pdf_table.py
_SO_LIEU_THUAN_RE = re.compile(r"^\d[\d.,]*\d$|^\d$")


def _la_so_lieu_thuan(v) -> bool:
    return bool(_SO_LIEU_THUAN_RE.match(_to_text(v)))


def split_header_body(rows: list[list]) -> tuple[list[list], list[list], bool]:
    """Tách khối HEADER (có thể nhiều dòng) khỏi THÂN bảng — spec §3.4.

    Bốn nhánh, THEO THỨ TỰ ưu tiên cho mỗi hàng CHƯA vào thân:
      1. `is_column_index_row` (TÁI DÙNG từ `xlsx_header.py`, không viết lại)
         → header. Kiểm TRƯỚC nhánh 3 vì một hàng đánh số cột cũng có ô số
         thuần nhưng KHÔNG BAO GIỜ là dữ liệu.
      2. Đa số ô (>50%) rỗng/None → header (dòng đệm/nhãn phụ thưa).
      3. Có ô không rỗng khớp mẫu SỐ LIỆU THUẦN (toàn chữ số + dấu phân
         cách, không lẫn chữ) → THÂN bắt đầu từ đây, mọi dòng sau (kể cả
         không khớp nhánh nào) đều là thân.
      4. Không khớp gì ở trên → VẪN header (mặc định — chỉ nhánh 3 mới
         chuyển sang thân, đừng đọc "không khớp" thành "vậy là thân").

    CHỐT AN TOÀN: nếu duyệt hết mà không hàng nào từng khớp nhánh 3 (0 hàng
    thân) — bảng toàn chữ, không có tín hiệu số nào — bảng không được biến
    mất. Còn >=2 hàng: dòng đầu làm header, phần còn lại làm thân. Đúng 1
    hàng: hàng đó tự nó thành thân, header rỗng (không có gì đứng trước nó
    để làm header)."""
    header: list[list] = []
    body: list[list] = []
    started = False
    for row in rows:
        if started:
            body.append(row)
            continue
        if is_column_index_row(row):
            header.append(row)
            continue
        filled = sum(1 for c in row if _to_text(c))
        if filled < len(row) / 2:
            header.append(row)
            continue
        if any(_la_so_lieu_thuan(c) for c in row):
            started = True
            body.append(row)
            continue
        header.append(row)
    if not body:
        if len(rows) <= 1:
            return [], rows[:], True
        return rows[:1], rows[1:], True
    return header, body, False
```

- [ ] **Step 9: Chạy lại test, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS

- [ ] **Step 10: Viết test cho `column_names` — loại hàng đánh số cột khỏi tên**

```python
from src.rag.pdf_table import column_names


def test_column_names_ghep_nhieu_dong_bo_qua_hang_danh_so_cot():
    # header trả về từ split_header_body của _BANG_BCTC ở Step 6 — GỒM CẢ
    # hàng đánh số cột ['1','2','3'] (nó thuộc header, không thuộc thân).
    header = _BANG_BCTC[:5]
    names = column_names(header)
    assert names == ["TÀI SẢN", "Thuyết minh", "Số cuối năm (3)"]


def test_column_names_cot_rong_dung_ten_vi_tri():
    header = [["", None], [None, "Giá trị"]]
    assert column_names(header) == ["Cột 1", "Giá trị"]


def test_column_names_header_rong_tra_danh_sach_rong():
    assert column_names([]) == []
```

- [ ] **Step 11: Chạy test, xác nhận FAIL, rồi viết `column_names`**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: FAIL với `ImportError`

```python
# Thêm vào backend/src/rag/pdf_table.py
def column_names(header_rows: list[list]) -> list[str]:
    """Tên cột = nối các ô KHÔNG RỖNG theo cột, theo thứ tự dòng header.

    LOẠI các hàng ĐÁNH SỐ CỘT khỏi phép nối — '1'/'2'/'3' không phải mảnh
    tên cột, ghép chúng vào sẽ sinh tên rác kiểu "TÀI SẢN 1"."""
    label_rows = [r for r in header_rows if not is_column_index_row(r)]
    if not label_rows:
        return []
    n_cols = len(label_rows[0])
    names = []
    for c in range(n_cols):
        parts = [_to_text(row[c]) for row in label_rows
                 if c < len(row) and _to_text(row[c])]
        names.append(" ".join(parts) if parts else f"Cột {c + 1}")
    return names
```

- [ ] **Step 12: Chạy lại test, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS

- [ ] **Step 13: Viết test + cài `row_to_text`**

```python
from src.rag.pdf_table import row_to_text


def test_row_to_text_tu_du_nghia():
    row = ["I. Tiền", "100", "1.234.567"]
    columns = ["TÀI SẢN", "Mã số", "Số cuối năm"]
    assert row_to_text(row, columns) == (
        "TÀI SẢN: I. Tiền | Mã số: 100 | Số cuối năm: 1.234.567")


def test_row_to_text_thieu_ten_cot_dung_vi_tri():
    assert row_to_text(["x", "y"], ["Tên"]) == "Tên: x | Cột 2: y"
```

```python
# Thêm vào backend/src/rag/pdf_table.py
def row_to_text(row: list, columns: list[str]) -> str:
    """'<Tên cột>: <giá trị> | ...' — mỗi hàng tự mang tên cột vì nó sẽ đứng
    RIÊNG một chunk (spec §3.4), khác `_bang_thanh_text` của .docx (cả bảng
    một block nên header đứng dòng đầu là đủ ngữ cảnh)."""
    parts = []
    for i, val in enumerate(row):
        name = columns[i] if i < len(columns) else f"Cột {i + 1}"
        parts.append(f"{name}: {_to_text(val)}")
    return " | ".join(parts)
```

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS (viết trước FAIL, sau khi thêm hàm thì PASS)

- [ ] **Step 14: Viết test + cài `checksum_gap`**

```python
from src.rag.pdf_table import checksum_gap


def test_checksum_gap_lien_mach_tra_none():
    body = [["1", "a"], ["2", "b"], ["3", "c"]]
    assert checksum_gap(body) is None


def test_checksum_gap_dut_doan_bao_ro_vi_tri():
    body = [["1", "a"], ["2", "b"], ["4", "d"]]
    assert checksum_gap(body) == "cột đếm đứt đoạn tại: 2→4"


def test_checksum_gap_khong_phai_cot_dem_im_lang_dung():
    body = [["Mã A", "x"], ["Mã B", "y"]]
    assert checksum_gap(body) is None


def test_checksum_gap_bang_rong_tra_none():
    assert checksum_gap([]) is None
```

```python
# Thêm vào backend/src/rag/pdf_table.py
def checksum_gap(body_rows: list[list]) -> str | None:
    """`None` nếu cột đầu KHÔNG phải số đếm liên tục (im lặng ĐÚNG — đa số
    bảng không có checksum, vd `bieumau_bctc_hopnhat.pdf`) hoặc liền mạch.
    Chuỗi mô tả nếu đứt đoạn — đi tiếp vào `Warning` qua cầu đã có ở KH2
    (spec §3.6)."""
    if not body_rows:
        return None
    col0 = [_to_text(row[0]) if row else "" for row in body_rows]
    if not all(re.fullmatch(r"\d+", v) for v in col0):
        return None
    nums = [int(v) for v in col0]
    gaps = [f"{nums[i]}→{nums[i + 1]}" for i in range(len(nums) - 1)
            if nums[i + 1] - nums[i] != 1]
    if not gaps:
        return None
    return f"cột đếm đứt đoạn tại: {', '.join(gaps)}"
```

Run: `cd backend && python -m pytest tests/rag/test_pdf_table.py -v`
Expected: PASS — tất cả test trong file (16 test)

- [ ] **Step 15: Commit**

```bash
git add backend/src/rag/pdf_table.py backend/tests/rag/test_pdf_table.py
git commit -m "feat(rag): pdf_table.py - thuat toan bang PDF thuan, tai dung is_column_index_row"
```

---

## Task 2: Nối `pdfplumber` vào `parse_pdf` — chỉ chạm trang có bảng

**Files:**
- Modify: `backend/src/rag/parse.py:406-435` (hàm `parse_pdf`)
- Modify: `backend/requirements.txt` (thêm `pdfplumber==0.11.10`)
- Test: `backend/tests/rag/test_parse_pdf.py`

**Interfaces:**
- Consumes: `merge_table_rows`, `split_header_body`, `column_names`, `row_to_text`,
  `checksum_gap` từ Task 1 (`backend/src/rag/pdf_table.py`).
- Produces (dùng ở Task 3, 4): `parse_pdf(path: str) -> tuple[list[dict], list[tuple[str, str]]]`
  — ĐỔI CHỮ KÝ từ `list[dict]` sang `tuple[list[dict], list[tuple[str, str]]]` (giống hệt khuôn
  `parse_xlsx`). Mỗi block bảng có thêm khoá `"atomic": True` (block văn xuôi KHÔNG có khoá này,
  giữ nguyên hình dạng cũ).

- [ ] **Step 1: Khai `pdfplumber` vào `requirements.txt`**

Đã cài "chui" trong `.venv` (0.11.10) — mở `backend/requirements.txt`, thêm ngay dưới dòng
`pypdf==5.1.0` (dòng 45):

```
pdfplumber==0.11.10
```

- [ ] **Step 2: Cài lại theo requirements đã khai, xác nhận không đổi phiên bản**

Run: `cd backend && .venv/Scripts/pip install -r requirements.txt`
Expected: `Requirement already satisfied: pdfplumber==0.11.10` (không tải lại gì — đã có sẵn)

- [ ] **Step 3: Viết test khoá bất biến "trang không bảng không đổi gì" — TRƯỚC khi sửa `parse_pdf`**

Đây là phép thử phá quan trọng nhất của B4 (spec §3.1, §6): phải ĐỎ nếu ai đó lỡ để
`pdfplumber` chạm vào trang không có bảng. Test hiện tại (`test_parse_pdf.py`) monkeypatch
`pypdf.PdfReader` nhưng KHÔNG monkeypatch `pdfplumber` — vì `parse_pdf` mới sẽ mở file bằng CẢ
hai thư viện, cần một fixture giả cho `pdfplumber.open()` trả "trang này không có bảng":

```python
# Thêm vào ĐẦU backend/tests/rag/test_parse_pdf.py, TRƯỚC class _FakePage hiện có
class _FakePlumberPage:
    """Giả trang `pdfplumber` KHÔNG có bảng nào — `find_tables()` rỗng."""
    def find_tables(self, table_settings=None):
        return []


class _FakePlumberPDF:
    def __init__(self, n_pages):
        self.pages = [_FakePlumberPage() for _ in range(n_pages)]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        return False


def _khong_bang(monkeypatch, n_pages=1):
    """Monkeypatch `pdfplumber.open` để MỌI trang báo 'không có bảng' — dùng
    cho các test hiện có, vốn chỉ kiểm logic heading/furniture trên pypdf,
    không liên quan gì tới bảng."""
    import pdfplumber
    monkeypatch.setattr(pdfplumber, "open",
                        lambda path: _FakePlumberPDF(n_pages))
```

Sửa CHỮ KÝ gọi `parse.parse_pdf(...)` trong 4 test hiện có (`test_parse_pdf_strips_nul_bytes...`,
`test_khoan_single_level_number_is_not_heading`, `test_multilevel_numeric_heading_still_detected`,
`test_money_and_hs_code_lines_are_not_headings`) để: (a) gọi `_khong_bang(monkeypatch)` trước dòng
gọi `parse.parse_pdf`, (b) unpack `blocks, warnings = parse.parse_pdf(...)` thay vì
`blocks = parse.parse_pdf(...)`. Ví dụ áp cho `test_khoan_single_level_number_is_not_heading`:

```python
def test_khoan_single_level_number_is_not_heading(monkeypatch):
    import pypdf
    from src.rag import parse
    fake = _FakeReader([
        "Điều 124. Trường hợp bị cưỡng chế thi hành quyết định hành chính về quản lý thuế\n"
        "1. Người nộp thuế có tiền thuế nợ quá 90 ngày kể từ ngày hết thời hạn nộp theo quy định."
    ])
    monkeypatch.setattr(pypdf, "PdfReader", lambda path: fake)
    _khong_bang(monkeypatch)
    blocks, warnings = parse.parse_pdf("x.pdf")
    assert warnings == []
    assert blocks[0]["heading_level"] == 4
    assert blocks[1]["heading_level"] is None
```

3 test còn lại — CÙNG khuôn, viết tường minh từng cái (chỉ thêm dòng `_khong_bang(monkeypatch)` và
đổi dòng gọi `parse_pdf` sang unpack tuple, mọi assertion khác giữ NGUYÊN):

```python
def test_parse_pdf_strips_nul_bytes_from_unmapped_glyphs(monkeypatch):
    import pypdf
    from src.rag import parse

    fake = _FakeReader(["\x00 Verify the delivered supplies against the PO."])
    monkeypatch.setattr(pypdf, "PdfReader", lambda path: fake)
    _khong_bang(monkeypatch)

    blocks, warnings = parse.parse_pdf("irrelevant.pdf")

    assert warnings == []
    assert len(blocks) == 1
    assert "\x00" not in blocks[0]["text"]
    assert blocks[0]["text"] == "Verify the delivered supplies against the PO."


def test_multilevel_numeric_heading_still_detected(monkeypatch):
    import pypdf
    from src.rag import parse
    fake = _FakeReader(["1.1 Giới thiệu hệ thống\n3.2.1. Cấu hình chi tiết"])
    monkeypatch.setattr(pypdf, "PdfReader", lambda path: fake)
    _khong_bang(monkeypatch)
    blocks, warnings = parse.parse_pdf("x.pdf")
    assert warnings == []
    assert blocks[0]["heading_level"] == 5
    assert blocks[1]["heading_level"] == 5


def test_money_and_hs_code_lines_are_not_headings(monkeypatch):
    import pypdf
    from src.rag import parse
    fake = _FakeReader(["5.000.000.000 đồng.\n2931.9080 77-81-6"])
    monkeypatch.setattr(pypdf, "PdfReader", lambda path: fake)
    _khong_bang(monkeypatch)
    blocks, warnings = parse.parse_pdf("x.pdf")
    assert warnings == []
    assert all(b["heading_level"] is None for b in blocks)
```

Các test furniture (`test_detect_...`) và test `_HEADING_RE` (`test_heading_that_van_duoc_nhan`...)
gọi thẳng `detect_page_furniture`/`_HEADING_RE`, KHÔNG gọi `parse_pdf` — không cần sửa.

- [ ] **Step 4: Chạy test, xác nhận FAIL (chữ ký cũ chưa đổi)**

Run: `cd backend && python -m pytest tests/rag/test_parse_pdf.py -v`
Expected: FAIL — `TypeError: cannot unpack non-iterable ...` hoặc `AttributeError: module
'pdfplumber' has no attribute 'open'` bị monkeypatch nhầm thứ tự (vì `parse_pdf` CHƯA gọi
`pdfplumber` — `_khong_bang` monkeypatch vẫn set được, nhưng unpack tuple sẽ FAIL vì hàm cũ trả
`list`, không phải `tuple`)

- [ ] **Step 5: Viết test cho một trang CÓ bảng — dùng file thật `ssc_bieumau.pdf`**

```python
# Cuối backend/tests/rag/test_parse_pdf.py
import os

import pytest

KHO = "d:/Youdoo/tmp-docs"


@pytest.mark.live
@pytest.mark.skipif(not os.path.isdir(KHO), reason="chưa có tmp-docs")
def test_parse_pdf_bang_that_moi_hang_la_mot_block_atomic():
    from src.rag.parse import parse_pdf
    blocks, warnings = parse_pdf(os.path.join(KHO, "ssc_bieumau.pdf"))
    bang_blocks = [b for b in blocks if b.get("atomic")]
    assert bang_blocks, "phải có ít nhất 1 block bảng atomic"
    # Mỗi block bảng tự mang tên cột — không phải chỉ số/giá trị trần.
    assert any(":" in b["text"] for b in bang_blocks)
    assert all(b["heading_level"] is None for b in bang_blocks)
    # Đo thật 2026-09-04 (find_tables() từng trang, xem ghi chú thực thi):
    # trang 1 có bảng TT đếm 1..4 liên mạch; trang 2-8 có bảng khác đánh số
    # PHÂN CẤP ("1", "1.1", "1.2"...) — cột đầu KHÔNG toàn chữ số thuần nên
    # checksum_gap() không áp dụng (im lặng đúng, không phải lỗ hổng). CẢ
    # HAI trường hợp đã đo đều không sinh cảnh báo, nên warnings==[] đúng
    # với 2 file mẫu đã đo — nếu implementer đo ra khác, SỬA assertion theo
    # số thật, đừng ép về [] cho khớp dòng này.
    assert warnings == []
```

- [ ] **Step 6: Chạy test mới, xác nhận FAIL (chưa cài logic bảng)**

Run: `cd backend && python -m pytest tests/rag/test_parse_pdf.py -v -m live`
Expected: FAIL — `blocks` chưa có `atomic` nào (hàm cũ chưa biết bảng)

- [ ] **Step 7: Viết `parse_pdf` mới**

Trước hết, thêm import vào ĐẦU `backend/src/rag/parse.py`, ngay dưới dòng `import pypdf` (dòng 8):

```python
import pdfplumber

from .pdf_table import (checksum_gap, column_names, merge_table_rows,
                        row_to_text, split_header_body)
```

Sau đó THAY THẾ hoàn toàn hàm `parse_pdf` hiện có (dòng 406-435) bằng các hàm sau (thêm 3 hàm phụ
trợ mới TRƯỚC `parse_pdf`, rồi `parse_pdf` mới thay cho bản cũ):

```python
def _lines_tu_text(text: str) -> list[str]:
    out = []
    for line in (text or "").splitlines():
        t = line.replace("\x00", "").strip()
        if t:
            out.append(t)
    return out


def _trich_mot_bang(plumber_page, bbox) -> tuple[list[list], list[list]]:
    """(hàng chế độ mặc định, hàng chế độ text) của MỘT vùng bbox trên trang.

    Đối chiếu bằng SỐ LƯỢNG bảng dò được trong đúng bbox này ở cả hai chế
    độ — không khớp thì bỏ chế độ `text` cho vùng này (an toàn hơn đoán sai
    tương ứng bảng nào với bảng nào, spec §3.3 chỉ nói gộp TRONG một bảng,
    không nói gộp bảng CHÉO NHAU)."""
    vung = plumber_page.within_bbox(bbox, relative=False)
    mac_dinh = vung.find_tables()
    theo_text = vung.find_tables(table_settings={"horizontal_strategy": "text"})
    hang_mac_dinh = mac_dinh[0].extract() if mac_dinh else []
    hang_text = theo_text[0].extract() if len(theo_text) == 1 else []
    return hang_mac_dinh, hang_text


def _khoi_bang(plumber_page, bang, pageno: int
               ) -> tuple[list[dict], list[tuple[str, str]]]:
    """Blocks + warnings của MỘT bảng đã dò được trên trang `pageno`."""
    x0, top, x1, bottom = bang.bbox
    hang_mac_dinh, hang_text = _trich_mot_bang(plumber_page, (x0, top, x1, bottom))
    if not hang_mac_dinh:
        hang_mac_dinh = bang.extract()
    gop = merge_table_rows(hang_mac_dinh, hang_text)
    tat_ca_hang = [r for r, _ in gop]
    header_rows, body_rows, _ = split_header_body(tat_ca_hang)
    columns = column_names(header_rows)
    blocks = [{"text": row_to_text(row, columns), "heading_level": None,
              "page": pageno, "atomic": True} for row in body_rows]
    warnings: list[tuple[str, str]] = []
    gap = checksum_gap(body_rows)
    if gap:
        warnings.append((f"trang {pageno}, bảng", gap))
    return blocks, warnings


def parse_pdf(path: str) -> tuple[list[dict], list[tuple[str, str]]]:
    """Heuristic headings (no font info): numbered/keyword headings & short
    ALL-CAPS lines. Trang CÓ bảng đi qua `pdfplumber` (spec 2026-09-04-b4);
    trang KHÔNG có bảng giữ NGUYÊN đường `pypdf` — bất biến byte-identical
    bắt buộc, xem test `test_trang_khong_bang_byte_identical`.

    HAI LƯỢT từ 2026-08-19: gom dòng theo trang trước, nhận diện rác
    header/footer trên toàn tài liệu, rồi mới dựng block. Một lượt thì không
    thể biết một dòng có lặp trên phần lớn số trang hay không.
    """
    reader = pypdf.PdfReader(path)
    all_warnings: list[tuple[str, str]] = []
    with pdfplumber.open(path) as pdf:
        pages: list[list[str]] = []
        page_bangs: list[list] = []
        for pageno, page in enumerate(reader.pages, start=1):
            plumber_page = pdf.pages[pageno - 1]
            bangs = sorted(plumber_page.find_tables(), key=lambda b: b.bbox[1])
            page_bangs.append(bangs)
            if not bangs:
                pages.append(_lines_tu_text(page.extract_text() or ""))
                continue
            width, height = plumber_page.width, plumber_page.height
            y_bien = [0.0] + [y for b in bangs for y in (b.bbox[1], b.bbox[3])] + [height]
            dai_lines: list[str] = []
            for k in range(0, len(y_bien), 2):
                y0, y1 = y_bien[k], y_bien[k + 1]
                if y1 > y0:
                    dai = plumber_page.within_bbox((0, y0, width, y1), relative=False)
                    dai_lines.extend(_lines_tu_text(dai.extract_text() or ""))
            pages.append(dai_lines)

        furniture = detect_page_furniture(pages)
        blocks: list[dict] = []
        for pageno, lines in enumerate(pages, start=1):
            plumber_page = pdf.pages[pageno - 1]
            bangs = page_bangs[pageno - 1]
            for text in lines:
                if _normalize_digits(text) in furniture:
                    continue
                blocks.append({"text": text, "heading_level": heading_level(text),
                               "page": pageno})
            for bang in bangs:
                bang_blocks, bang_warnings = _khoi_bang(plumber_page, bang, pageno)
                blocks.extend(bang_blocks)
                all_warnings.extend(bang_warnings)
    return blocks, all_warnings
```

**Lưu ý cho người thi hành**: đoạn "chia dải theo chiều dọc" (`y_bien`) chỉ TÁCH văn bản khỏi
bảng để nhận diện heading/furniture — nó KHÔNG chèn hàng bảng vào đúng vị trí xen giữa các dải văn
xuôi theo thứ tự đọc thật (bảng luôn được thêm vào CUỐI trang ở vòng lặp thứ hai). Đây là ĐƠN GIẢN
HOÁ có chủ ý so với spec §3.2 (spec mô tả chèn xen kẽ đúng vị trí) — chấp nhận vì: (a) mọi corpus
thật đã khảo sát (`ssc_bieumau.pdf`, `bieumau_bctc_hopnhat.pdf`, hoá đơn) không có văn xuôi Ý NGHĨA
xen giữa hai bảng trên cùng một trang cần giữ đúng vị trí tương đối với bảng, (b) breadcrumb/section
vẫn đúng vì `heading_level()` chạy trên đúng dải TRƯỚC bảng nếu có heading ở đó. Nếu đo thấy corpus
khác cần thứ tự chèn chính xác, đó là việc của round sau — ghi vào ghi chú thực thi ở Task 5, không
chặn B4.

- [ ] **Step 8: Chạy lại toàn bộ test file, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_parse_pdf.py -v`
Expected: PASS toàn bộ (kể cả test `live` nếu `d:/Youdoo/tmp-docs` tồn tại; SKIPPED nếu không)

- [ ] **Step 9: Viết test khoá bất biến "gate có tác dụng đo được" bằng ĐỘT BIẾN — phép thử phá bắt buộc**

Hai gate của §3.1 cần CHỨNG MINH riêng, không phải một: (a) trang KHÔNG có bảng phải giữ nguyên
đường `pypdf` — đã chứng minh gián tiếp bởi 4 test hiện có ở Step 3, giờ chạy với `_khong_bang()`
và VẪN khớp đúng giá trị `heading_level`/`text` như trước B4 (không đổi assertion nào); (b) trang
CÓ bảng phải THỰC SỰ rẽ nhánh, không phải rơi qua đường `pypdf` như không có gì xảy ra. Step này
chứng minh (b) — thiếu nó, một lỗi kiểu điều kiện viết ngược (`if bangs: ...pypdf...` thay vì
`if not bangs`) vẫn lọt qua mọi test khác vì (a) không phát hiện được lỗi RẼ NHẦM NHÁNH, chỉ phát
hiện được lỗi "nhánh không-bảng bị đụng vào".

Test dùng MỘT "bảng" giả PHỦ HẾT TRANG (bbox = cả trang, không hàng nào) — không cần dựng lại toàn
bộ đường trích bảng thật (đã kiểm bằng file thật ở Step 5), chỉ cần buộc `bangs` khác rỗng. Nếu
nhánh "có bảng" thực sự chạy, dòng văn xuôi từ `pypdf` ("Điều 1. ...") phải BIẾN MẤT khỏi output
(thay bằng dải-text rỗng, vì cả trang bị coi là vùng bảng và không dải nào có chiều cao dương).
Phép thử phá: đảo điều kiện `if not bangs:` (Step 7) thành `if bangs:` — test này phải ĐỎ, vì lúc
đó trang có bảng lại đi qua đường `pypdf` nguyên bản, dòng văn xuôi không biến mất.

```python
# Cuối backend/tests/rag/test_parse_pdf.py
class _FakeBangToanTrang:
    """Một 'bảng' giả có bbox = TOÀN TRANG, không hàng nào — đủ để buộc
    parse_pdf rẽ sang nhánh 'trang có bảng', không cần mô phỏng đúng cấu
    trúc bảng thật (đã kiểm bằng file thật ở Step 5)."""
    def __init__(self, w, h):
        self.bbox = (0, 0, w, h)

    def extract(self):
        return []


class _FakePlumberPageCoBang:
    width, height = 100, 100

    def find_tables(self, table_settings=None):
        # Chế độ mặc định (table_settings=None) thấy 1 bảng phủ hết trang;
        # chế độ text không thấy gì — không cần khớp nhau cho test này.
        return [_FakeBangToanTrang(self.width, self.height)] if table_settings is None else []

    def within_bbox(self, bbox, relative=False):
        return self

    def extract_text(self):
        return ""


class _FakePlumberPDFCoBang:
    def __init__(self):
        self.pages = [_FakePlumberPageCoBang()]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        return False


def test_trang_co_bang_phu_het_thi_dong_van_xuoi_pypdf_bien_mat(monkeypatch):
    import pypdf
    import pdfplumber
    from src.rag import parse
    fake = _FakeReader(["Điều 1. Nội dung không liên quan bảng."])
    monkeypatch.setattr(pypdf, "PdfReader", lambda path: fake)
    monkeypatch.setattr(pdfplumber, "open", lambda path: _FakePlumberPDFCoBang())

    blocks, _ = parse.parse_pdf("x.pdf")
    van_xuoi = [b for b in blocks if not b.get("atomic")]
    assert van_xuoi == [], (
        "trang có bảng phủ hết trang vẫn còn dòng văn xuôi từ pypdf — "
        "gate 'chỉ chạm trang có bảng' không có tác dụng đo được")
```

- [ ] **Step 10: Chạy test đột biến, xác nhận PASS (chứng minh gate có tác dụng)**

Run: `cd backend && python -m pytest tests/rag/test_parse_pdf.py::test_trang_co_bang_phu_het_thi_dong_van_xuoi_pypdf_bien_mat -v`
Expected: PASS

- [ ] **Step 11: Chạy toàn bộ suite nhanh, xác nhận không hồi quy nơi khác**

Run: `cd backend && python -m pytest -m "not integration and not live" -q`
Expected: PASS, tổng số test TĂNG so với baseline 2305 (các test mới ở Task 1 + Task 2)

- [ ] **Step 12: Commit**

```bash
git add backend/src/rag/parse.py backend/requirements.txt backend/tests/rag/test_parse_pdf.py
git commit -m "feat(rag): parse_pdf noi pdfplumber, chi cham trang co bang (B4)"
```

---

## Task 3: `chunking.py` — block "nguyên tử" không bị gộp/cắt câu

**Files:**
- Modify: `backend/src/rag/chunking.py:51-120` (hàm `chunk_text_blocks`)
- Test: `backend/tests/rag/test_chunking_atomic.py`

**Interfaces:**
- Consumes: block dict có khoá tuỳ chọn `"atomic": bool` (Task 2 đặt `True` cho block bảng PDF;
  mọi caller khác — `.docx`/`.pptx` — không đặt khoá này, mặc định coi như `False`).
- Produces: `chunk_text_blocks` giữ NGUYÊN chữ ký `(blocks, *, doc_id, source_file) -> list[dict]`
  — không đổi gì phía gọi hàm.

- [ ] **Step 1: Viết test khoá hành vi CŨ — `.docx`/`.pptx` không đổi (không có khoá `atomic`)**

```python
# backend/tests/rag/test_chunking_atomic.py
from src.rag.chunking import chunk_text_blocks


def test_block_khong_co_khoa_atomic_hanh_vi_nhu_cu():
    blocks = [
        {"text": "Điều 1. Tiêu đề", "heading_level": 4, "page": None},
        {"text": "Câu một. Câu hai.", "heading_level": None, "page": None},
    ]
    out = chunk_text_blocks(blocks, doc_id="d1", source_file="f.docx")
    assert len(out) == 1
    assert out[0]["chunk_text"] == "Câu một. Câu hai."
```

- [ ] **Step 2: Chạy test, xác nhận PASS ngay (test khoá hành vi hiện có, chưa sửa gì)**

Run: `cd backend && python -m pytest tests/rag/test_chunking_atomic.py -v`
Expected: PASS

- [ ] **Step 3: Viết test cho block atomic — LUÔN thành 1 chunk riêng, không gộp với văn xuôi liền kề**

```python
def test_block_atomic_luon_mot_chunk_rieng_du_ngan():
    blocks = [
        {"text": "Điều 1. Bảng thuế", "heading_level": 4, "page": None},
        {"text": "Mã: A | Mức: 10%", "heading_level": None, "page": 1, "atomic": True},
        {"text": "Mã: B | Mức: 5%", "heading_level": None, "page": 1, "atomic": True},
        {"text": "Ghi chú ngắn.", "heading_level": None, "page": None},
    ]
    out = chunk_text_blocks(blocks, doc_id="d1", source_file="f.pdf")
    texts = [c["chunk_text"] for c in out]
    assert "Mã: A | Mức: 10%" in texts
    assert "Mã: B | Mức: 5%" in texts
    # Hai hàng KHÔNG được gộp chung một chunk dù cộng dồn rất ngắn.
    assert not any("Mã: A" in t and "Mã: B" in t for t in texts)
    assert "Ghi chú ngắn." in texts


def test_block_atomic_khong_di_qua_split_section_text_du_vuot_ngan_sach(monkeypatch):
    from src.rag import chunking
    # Ép ngân sách token cực nhỏ để mô phỏng "hàng vượt CHUNK_SIZE_TOKENS".
    monkeypatch.setattr(chunking, "CHUNK_SIZE_TOKENS", 1)
    blocks = [{"text": "Mã: A | Mô tả: một câu khá dài để vượt ngân sách token nhỏ này",
              "heading_level": None, "page": 1, "atomic": True}]
    out = chunk_text_blocks(blocks, doc_id="d1", source_file="f.pdf")
    assert len(out) == 1
    assert out[0]["chunk_text"] == blocks[0]["text"]
```

- [ ] **Step 4: Chạy test, xác nhận FAIL — block atomic hiện bị gộp/cắt như văn xuôi thường**

Run: `cd backend && python -m pytest tests/rag/test_chunking_atomic.py -v`
Expected: FAIL — `test_block_atomic_luon_mot_chunk_rieng_du_ngan` fail vì hai hàng bị gộp chung 1
chunk (cộng dồn token vẫn dưới `CHUNK_SIZE_TOKENS`).

- [ ] **Step 5: Đọc `chunk_text_blocks` hiện tại để định vị đúng chỗ sửa**

`chunk_text_blocks` (`backend/src/rag/chunking.py:51-120`) có 2 giai đoạn: (a) vòng lặp
`for b in blocks` dựng `sections: list[tuple[section_path, page, body: list[str]]]`, `body` hiện
là MỘT `list[str]` PHẲNG chứa mọi block-text của một mục (dòng 62, `cur_body: list[str] = []`,
`_flush()` ở dòng 65-76 append `cur_body[:]` nguyên khối); (b) vòng lặp `for section_path, page,
body in sections` ở dòng 105 nối `" ".join(body)` rồi cắt cửa sổ. Sửa CẢ HAI giai đoạn: giai đoạn
(a) đổi `cur_body` từ `list[str]` sang `list[tuple[str, bool]]` (text, atomic) để mang cờ theo tới
giai đoạn (b); giai đoạn (b) tách `body` thành các RUN trước khi xử lý.

- [ ] **Step 6: Sửa `chunk_text_blocks`**

```python
# Thay thế backend/src/rag/chunking.py:51-120
def chunk_text_blocks(blocks: list[dict], *, doc_id: str, source_file: str) -> list[dict]:
    """Structure-aware: group body under its heading path, chunk within a leaf section only."""
    doc_title = next((b["text"] for b in blocks if b["heading_level"]),
                     os.path.basename(source_file))
    path_stack: list[tuple[int, str]] = []
    sections: list[tuple[str, int | None, list[tuple[str, bool]]]] = []
    cur_body: list[tuple[str, bool]] = []
    cur_page: int | None = None

    def _flush():
        if cur_body:
            crumb = " › ".join(t for _, t in path_stack)
            sections.append((crumb, cur_page, cur_body[:]))

    for b in blocks:
        atomic = bool(b.get("atomic"))
        if b["heading_level"]:
            lvl = b["heading_level"]
            if not cur_body and path_stack and path_stack[-1][0] >= lvl:
                if cur_page is None:
                    cur_page = b["page"]
                cur_body.append((b["text"], atomic))
                continue
            _flush()
            cur_body.clear()
            while path_stack and path_stack[-1][0] >= lvl:
                path_stack.pop()
            path_stack.append((lvl, b["text"]))
            cur_page = b["page"]
        else:
            if cur_page is None:
                cur_page = b["page"]
            cur_body.append((b["text"], atomic))
    _flush()

    out: list[dict] = []
    idx = 0
    for section_path, page, body in sections:
        # Tách thành các RUN: văn xuôi liên tục (không atomic) hoặc một
        # block atomic đứng riêng — atomic KHÔNG được gộp với run liền kề dù
        # cộng dồn vẫn dưới CHUNK_SIZE_TOKENS (spec 2026-09-04-b4 §3.5).
        pieces: list[str] = []
        run: list[str] = []
        for text, atomic in body:
            if atomic:
                if run:
                    joined = " ".join(run).strip()
                    if joined:
                        pieces.extend([joined] if count_tokens(joined) <= MIN_CHUNK_TOKENS
                                      else _split_section_text(joined))
                    run = []
                pieces.append(text)
            else:
                run.append(text)
        if run:
            joined = " ".join(run).strip()
            if joined:
                pieces.extend([joined] if count_tokens(joined) <= MIN_CHUNK_TOKENS
                              else _split_section_text(joined))
        for piece in pieces:
            out.append({
                "doc_id": doc_id, "source_file": source_file, "doc_title": doc_title,
                "section_path": section_path, "page": page,
                "sheet": None, "row_range": None, "columns": None,
                "chunk_index": idx, "token_count": count_tokens(piece),
                "chunk_text": piece,
            })
            idx += 1
    return out
```

- [ ] **Step 7: Chạy lại test file, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_chunking_atomic.py -v`
Expected: PASS toàn bộ

- [ ] **Step 8: Chạy TOÀN BỘ test hiện có của `chunking.py` — khoá bất biến ".docx/.pptx không đổi"**

Run: `cd backend && python -m pytest tests/rag/test_chunking.py tests/rag/test_chunking_khong_duong_dan.py -v`
Expected: PASS toàn bộ, KHÔNG một test nào trong hai file này (viết trước B4, phủ `.docx`/`.pptx`)
bị đổi kết quả — đây là bằng chứng trực tiếp "khoá `atomic` không đổi hành vi cũ" (Global
Constraints).

- [ ] **Step 9: Chạy toàn bộ suite nhanh**

Run: `cd backend && python -m pytest -m "not integration and not live" -q`
Expected: PASS, KHÔNG có test nào NGOÀI các file vừa sửa/thêm bị đổi kết quả.

- [ ] **Step 10: Commit**

```bash
git add backend/src/rag/chunking.py backend/tests/rag/test_chunking_atomic.py
git commit -m "feat(rag): chunk_text_blocks nhan dien block atomic, khong gop/cat cau (B4)"
```

---

## Task 4: `ingest.py` — nối `parse_pdf` mới + cảnh báo checksum qua Tầng A

**Files:**
- Modify: `backend/src/rag/ingest.py:78-106` (hàm `_chunks_for`)
- Test: `backend/tests/rag/test_ingest_pdf_warnings.py`

**Interfaces:**
- Consumes: `parse_pdf(path: str) -> tuple[list[dict], list[tuple[str, str]]]` (Task 2, chữ ký ĐÃ
  ĐỔI — mọi lời gọi `parse_pdf` khác trong codebase cũng phải cập nhật, xem Step 1).
- Produces: `_chunks_for` giữ NGUYÊN chữ ký hiện có
  `(read_path, kind, doc_id, *, source_file) -> tuple[list[dict], list[tuple[str, str]]]` — nhánh
  `.pdf` giờ trả `pdf_warnings` thay vì `[]` cứng.

- [ ] **Step 1: Tìm MỌI lời gọi `parse_pdf` trong codebase (chữ ký vừa đổi ở Task 2)**

Run: `cd backend && grep -rn "parse_pdf(" src/ tests/`
Expected: thấy `src/rag/ingest.py:99` (`blocks = parse_pdf(read_path)` trong `_chunks_for`) và các
lời gọi trong `tests/rag/test_parse_pdf.py` (đã sửa ở Task 2). Nếu có lời gọi khác ngoài hai chỗ
này, ghi lại — cần sửa TẤT CẢ, không chỉ `ingest.py`.

- [ ] **Step 2: Viết test cho `_chunks_for` — cảnh báo PDF chảy đúng vào tuple trả về**

```python
# backend/tests/rag/test_ingest_pdf_warnings.py
from src.rag.ingest import _chunks_for


def test_chunks_for_pdf_tra_ve_warnings_tu_parse_pdf(monkeypatch, tmp_path):
    from src.rag import ingest

    def _fake_parse_pdf(path):
        return (
            [{"text": "Mã: A | Mức: 10%", "heading_level": None, "page": 1,
              "atomic": True}],
            [("trang 1, bảng", "cột đếm đứt đoạn tại: 2→4")],
        )

    monkeypatch.setattr(ingest, "parse_pdf", _fake_parse_pdf)
    chunks, warnings = _chunks_for("x.pdf", "text", "doc1", source_file="x.pdf")
    assert warnings == [("trang 1, bảng", "cột đếm đứt đoạn tại: 2→4")]
    assert len(chunks) == 1
```

- [ ] **Step 3: Chạy test, xác nhận FAIL — `_chunks_for` hiện hard-code `[]` cho nhánh `.pdf`**

Run: `cd backend && python -m pytest tests/rag/test_ingest_pdf_warnings.py -v`
Expected: FAIL — `warnings == []` thay vì cảnh báo giả lập, HOẶC lỗi unpack nếu `parse_pdf` thật
(chưa mock đủ) được gọi và trả tuple 2 phần tử không khớp cách hàm cũ xử lý `blocks = parse_pdf(...)`

- [ ] **Step 4: Sửa `_chunks_for`**

```python
# backend/src/rag/ingest.py:78-106 — thay thế nhánh .pdf trong _chunks_for
def _chunks_for(read_path: str, kind: str, doc_id: str,
                source_file: str) -> tuple[list[dict], list[tuple[str, str]]]:
    """`read_path` là tệp ĐỌC nội dung ... [docstring hiện có giữ nguyên]"""
    if kind == "xlsx":
        sheets, sheet_warnings = parse_xlsx(read_path)
        return (chunk_xlsx_sheets(sheets, doc_id=doc_id, source_file=source_file),
                sheet_warnings)
    low = read_path.lower()
    if low.endswith(".pdf"):
        blocks, pdf_warnings = parse_pdf(read_path)
    elif low.endswith(".pptx"):
        blocks, pdf_warnings = parse_pptx(read_path), []
    else:
        blocks, pdf_warnings = parse_docx(read_path), []
    if not blocks:
        return [], []
    return (chunk_text_blocks(blocks, doc_id=doc_id, source_file=source_file),
            pdf_warnings)
```

- [ ] **Step 5: Chạy lại test, xác nhận PASS**

Run: `cd backend && python -m pytest tests/rag/test_ingest_pdf_warnings.py -v`
Expected: PASS

- [ ] **Step 6: Chạy toàn bộ test của `ingest.py` — khoá bất biến `.docx`/`.pptx`/`.xlsx` không đổi**

Run: `cd backend && python -m pytest tests/rag/test_ingest*.py -v`
Expected: PASS toàn bộ, không hồi quy.

- [ ] **Step 7: Chạy toàn bộ suite nhanh**

Run: `cd backend && python -m pytest -m "not integration and not live" -q`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add backend/src/rag/ingest.py backend/tests/rag/test_ingest_pdf_warnings.py
git commit -m "feat(rag): _chunks_for noi canh bao checksum PDF vao Warning co san (B4)"
```

---

## Task 5: Nghiệm thu trên corpus thật + ghi chú thực thi

**Files:**
- Test: `backend/tests/rag/test_pdf_kho_that.py`
- Modify: `docs/superpowers/specs/2026-08-31-tang-nap-tai-lieu-ghi-chu-thuc-thi.md` (nối thêm phần
  "B4 — PDF bóc bảng")

**Interfaces:**
- Consumes: `parse_pdf`, `chunk_text_blocks` (Task 2-3), toàn bộ pipeline `ingest._chunks_for`.
- Produces: không có API mới — đây là task NGHIỆM THU, khoá kết quả bằng test trên corpus thật.

- [ ] **Step 1: Test tự kiểm số học trên 100 `invoice_*.pdf`**

```python
# backend/tests/rag/test_pdf_kho_that.py
import os
import re

import pytest

from src.rag.parse import parse_pdf
from src.rag.chunking import chunk_text_blocks

KHO = "d:/Youdoo/tmp-docs"
pytestmark = pytest.mark.live


def _hoa_don():
    if not os.path.isdir(KHO):
        return []
    return sorted(f for f in os.listdir(KHO) if f.startswith("invoice_") and f.endswith(".pdf"))


@pytest.mark.skipif(not _hoa_don(), reason="chưa có tmp-docs")
def test_hoa_don_tu_kiem_so_hoc_khong_chunk_nao_lan_hang_khac():
    """Số lượng × đơn giá = thành tiền — tự kiểm được không cần đáp án tay
    (đúng khuôn Tầng C spec gốc §6). Kiểm bằng cách đọc lại 3 con số TỪ
    CÙNG MỘT chunk-hàng: nếu bảng bị gắn số sang hàng bên cạnh, phép nhân
    sẽ KHÔNG khớp."""
    sai = []
    for ten in _hoa_don():
        blocks, _ = parse_pdf(os.path.join(KHO, ten))
        chunks = chunk_text_blocks(blocks, doc_id=ten, source_file=ten)
        for c in chunks:
            t = c["chunk_text"]
            m_sl = re.search(r"Qty:\s*([\d.,]+)", t)
            m_gia = re.search(r"Net Price:\s*([\d.,]+)", t)
            m_tien = re.search(r"Net Worth:\s*([\d.,]+)", t)
            if not (m_sl and m_gia and m_tien):
                continue
            sl = float(m_sl.group(1).replace(",", ""))
            gia = float(m_gia.group(1).replace(",", ""))
            tien = float(m_tien.group(1).replace(",", ""))
            if abs(sl * gia - tien) > 0.5:
                sai.append((ten, t))
    assert sai == [], f"{len(sai)} hàng tự kiểm sai: {sai[:5]}"
```

- [ ] **Step 2: Chạy test trên corpus thật, GHI LẠI kết quả thật (không đoán trước)**

Run: `cd backend && python -m pytest tests/rag/test_pdf_kho_that.py::test_hoa_don_tu_kiem_so_hoc_khong_chunk_nao_lan_hang_khac -v -m live`
Expected: chạy thật, ghi số liệu THẬT vào ghi chú thực thi ở Step 6 (KHÔNG giả định PASS —
nếu FAIL, đọc lý do: có thể tên cột tiếng Anh không khớp mẫu regex trên vài hoá đơn có layout khác,
sửa regex hoặc bảng `_hoa_don()` cho tới khi đo đúng, đừng nới lỏng phép so sánh số học)

- [ ] **Step 3: Test checksum trên `ssc_bieumau.pdf` + đo hàng trùng lặp qua ranh giới trang**

```python
@pytest.mark.skipif(not os.path.isdir(KHO), reason="chưa có tmp-docs")
def test_ssc_bieumau_checksum_khong_dut_doan():
    _, warnings = parse_pdf(os.path.join(KHO, "ssc_bieumau.pdf"))
    dut_doan = [w for w in warnings if "đứt đoạn" in w[1]]
    assert dut_doan == [], f"checksum đứt đoạn không mong đợi: {dut_doan}"
```

**Phát hiện thật khi khảo sát kế hoạch này (2026-09-04)**, cần ĐO LẠI ở bước này, không giả định:
`ssc_bieumau.pdf` có một bảng đánh số phân cấp ("1", "1.1", "1.2"...) trải từ trang 3 đến trang 8
— `pdfplumber.find_tables()` dò bảng RIÊNG trên TỪNG TRANG, và đo trực tiếp cho thấy hàng cuối của
bảng trang 4 (`"2.2"`) trùng với hàng ĐẦU của bảng trang 5 (cùng `"2.2"`) — tức bảng bị "vỡ trang"
theo cách NGƯỢC với `bieumau_bctc_hopnhat.pdf` trang 21 (nơi mất hàng): ở đây một hàng có thể bị
LẶP hai lần thay vì mất. `parse_pdf` xử lý bảng ĐỘC LẬP theo từng trang nên không tự phát hiện được
ca này. Viết thêm một phép đo (không phải test gate cứng, vì chưa biết chắc mức độ phổ biến):

```python
def test_ssc_bieumau_do_hang_trung_lap_qua_trang(capsys):
    blocks, _ = parse_pdf(os.path.join(KHO, "ssc_bieumau.pdf"))
    texts = [b["text"] for b in blocks if b.get("atomic")]
    trung = [t for t in set(texts) if texts.count(t) > 1]
    print(f"\n[B4] {len(trung)} hàng bảng TRÙNG LẶP trong ssc_bieumau.pdf: {trung[:3]}")
```

Chạy, GHI SỐ THẬT (không phải 0 giả định) vào ghi chú thực thi ở Step 6 — đây là giới hạn CHẤP
NHẬN của B4 (spec §3.4 đã chấp nhận "bảng vỡ trang" cho ca MẤT hàng; ca LẶP hàng là biến thể của
CÙNG giới hạn gốc — `pdfplumber` không nối bảng qua trang — không phải bug mới của `merge_table_rows`
hay `split_header_body`). Không cần vá trong B4; chỉ cần ĐO và GHI, đúng nguyên tắc "hỏng lớn tiếng
còn hơn thiếu âm thầm" — im lặng bỏ qua phát hiện này mới là lỗi.

- [ ] **Step 4: Test "không chunk nào lẫn mã cột đầu của hàng khác" trên `bieumau_bctc_hopnhat.pdf`**

```python
@pytest.mark.skipif(not os.path.isdir(KHO), reason="chưa có tmp-docs")
def test_bctc_khong_chunk_nao_lan_ma_cot_dau_hang_khac():
    blocks, _ = parse_pdf(os.path.join(KHO, "bieumau_bctc_hopnhat.pdf"))
    chunks = chunk_text_blocks(blocks, doc_id="bctc", source_file="bctc.pdf")
    ma_dau_re = re.compile(r"^[^:]+:\s*([^|]+)")
    hang_bang = [c for c in chunks
                if any(k in c["chunk_text"] for k in (" | ", ": "))
                and ma_dau_re.match(c["chunk_text"])]
    # Mỗi chunk-hàng tự đủ nghĩa: đúng MỘT giá trị cột đầu, không phải danh
    # sách nhiều giá trị (dấu hiệu lẫn hàng nếu ô đầu chứa nhiều số/mã cách
    # nhau bởi khoảng trắng kép hoặc dấu phẩy nối).
    lan = [c["chunk_text"] for c in hang_bang
          if len(re.findall(r"\d[\d.,]{2,}", ma_dau_re.match(c["chunk_text"]).group(1))) > 1]
    assert lan == [], f"{len(lan)} chunk nghi lẫn số hàng khác: {lan[:5]}"
```

- [ ] **Step 5: Chạy Step 3 + Step 4, ghi số liệu thật**

Run: `cd backend && python -m pytest tests/rag/test_pdf_kho_that.py -v -m live`
Expected: chạy thật trên corpus, ghi kết quả (PASS/FAIL + số liệu) vào ghi chú thực thi

- [ ] **Step 6: Nối phần "B4 — PDF bóc bảng" vào ghi chú thực thi CHUNG**

Mở `docs/superpowers/specs/2026-08-31-tang-nap-tai-lieu-ghi-chu-thuc-thi.md`, thêm mục mới ở CUỐI
file (theo đúng khuôn các mục "kế hoạch 1"/"kế hoạch 2"/"B3" đã có): kết quả THẬT của Step 2/5 (số
hàng tự kiểm đúng/sai trên 100 hoá đơn, checksum `ssc_bieumau.pdf`, kết quả lẫn-số trên
`bieumau_bctc_hopnhat.pdf`), quyết định đơn giản hoá "không chèn bảng đúng vị trí xen giữa dải văn
xuôi" đã ghi ở Task 2 Step 7 (khó khăn/hướng đã chọn/giới hạn còn lại — bắt buộc theo
`feedback_note_difficulties_per_phase`), và bất kỳ ruling nào phát sinh khi Step 2 KHÔNG pass ngay
lần đầu.

- [ ] **Step 7: Chạy 683 trang PDF luật hiện có qua `parse_pdf` — xác nhận KHÔNG trang nào có bảng
      (tiền đề của gate byte-identical ở Task 2), ghi số liệu**

```bash
cd backend && python -c "
import glob
from src.rag.parse import parse_pdf
for f in glob.glob('d:/Youdoo/tmp-docs/*.pdf') + glob.glob('d:/Documents/luat-*.pdf'):
    blocks, warnings = parse_pdf(f)
    n_bang = sum(1 for b in blocks if b.get('atomic'))
    print(f, len(blocks), 'blocks,', n_bang, 'atomic (bang)')
"
```

Ghi kết quả thật vào ghi chú thực thi (Step 6) — đặc biệt file nào KHÔNG có bảng phải cho `n_bang
== 0`, đúng tiền đề gate byte-identical.

- [ ] **Step 8: Chạy toàn bộ suite nhanh lần cuối, ghi số suite trước/sau**

Run: `cd backend && python -m pytest -m "not integration and not live" -q`
Expected: PASS, tổng số test > baseline 2305 (Task 1-4 đã thêm test). Ghi số cuối cùng vào ghi chú
thực thi.

- [ ] **Step 9: Commit**

```bash
git add backend/tests/rag/test_pdf_kho_that.py docs/superpowers/specs/2026-08-31-tang-nap-tai-lieu-ghi-chu-thuc-thi.md
git commit -m "test(rag): nghiem thu B4 tren corpus PDF that + ghi chu thuc thi"
```
