langchain-core==1.4.8
langchain-openai==1.3.3
langchain-google-genai==4.2.0
langchain==1.2.18
httpx==0.28.1
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
tiktoken==0.13.0
python-dotenv==1.2.2
pytest==9.1.1
pytest-asyncio==1.4.0
langgraph==1.1.10
langgraph-checkpoint==4.1.0
langgraph-checkpoint-postgres==3.1.0
langgraph-prebuilt==1.0.13
langchain-mcp-adapters==0.3.0
# mcp: langchain-mcp-adapters==0.3.0 khai báo mcp>=1.9.2 không có cận trên,
# nên resolve không ghim sẽ chọn mcp mới nhất (2.0.0) — bản này đổi tên/dời
# mcp.shared.context.RequestContext, làm vỡ import của
# langchain_mcp_adapters.callbacks ngay lúc collection (Task 13). Ghim cùng
# 1.28.0 đã dùng ở mcp-servers/odoo/requirements.txt (Task 7), đã xác nhận
# tương thích cho cả phía client (venv này) lẫn phía server.
mcp==1.28.0
pgvector==0.4.1
# ─── Cross-encoder reranker (2026-08-19) ────────────────────────────────────
# ĐẢO quyết định 2026-07-12 ("không ghim torch/transformers, production không
# cần dep nặng"). Lý do đảo: trong venv thật hai gói này không có, nên _load()
# ném ModuleNotFoundError → fail-open nuốt → reranker CHẾT 100% im lặng kể từ
# lúc port sang Youdoo. Đo 2026-08-19: retrieve() trên corpus 3300 chunk trả
# method="hybrid-rrf" (không "+rerank"). Một tính năng chỉ tồn tại khi dep của
# nó tồn tại — để ngoài requirements là để nó chết.
#
# Bản +cu128 CHỦ Ý, không phải bản CPU mặc định: máy dev có RTX 5060 Ti
# (sm_120/Blackwell) và chỉ build cu128 mới có kernel cho kiến trúc này —
# đã xác nhận sm_120 nằm trong torch.cuda.get_arch_list().
# Đo thật, 20 cặp, model đã nóng:  GPU 21ms  |  CPU 987ms  (47×).
# Trên CPU reranker cộng ~1s vào MỌI câu hỏi tài liệu; trên GPU nó gần như
# miễn phí. Đó là chỗ ~2.5GB wheel mua được.
#
# Máy không NVIDIA vẫn chạy được: _resolve_device() lùi về "cpu" (hoặc ép
# bằng RERANK_DEVICE=cpu), và wheel cu128 vẫn import bình thường.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
transformers==5.15.0
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
pyvi==0.1.1
langfuse==4.14.1
fastapi==0.141.1
uvicorn==0.46.0
# PyYAML: đang có sẵn như dep bắc cầu (langchain), nay là dep TRỰC TIẾP —
# skill_manifest.py parse frontmatter SKILL.md bằng yaml.safe_load. Ghim
# tường minh để một lần gỡ dep bắc cầu không lặng lẽ làm app không lên.
PyYAML==6.0.3
