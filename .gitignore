# ─── Bí mật — KHÔNG BAO GIỜ commit ───────────────────────────────────────────
# models.csv chứa API key thật của 3 provider. Nó nằm trong repo vì tiện tra
# cứu tại chỗ, nhưng giá trị thật thuộc về .env; bảng hạn mức (không có key)
# thuộc về docs/provider-quotas.md — xem spec SP-1 §9.
.env
.env.*
!.env.example
models.csv
*.key
*.pem

# ─── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
.venv-*/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ─── Dữ liệu chạy / kết quả ──────────────────────────────────────────────────
logs/
*.log
.coverage
htmlcov/

# ─── IDE / OS ────────────────────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini

# ─── Claude Code — worktree/scratch state, không phải nội dung repo ─────────
.claude/worktrees/
.superpowers/

# --- Tai lieu test boc tach bang -- file that, KHONG commit ---
tmp-docs/

# --- Rac test co tu TRUOC dot OCR bac 1 (review toan nhanh B6) --------------
backend/suite_quick_result.txt

# --- Dataset ben thu 3 clone ve de doi chieu (vd VLegal-Bench, CC BY-NC-ND
# 4.0 -- cam tao derivative work). CHI ban goc khong sua duoc o day; ket qua
# phan loai/chia phai tinh lai tu kich ban, KHONG luu thanh tep. Xem docstring
# backend/evals/vlegal_bench_roles.py. ------------------------------------
backend/evals/external-data/
